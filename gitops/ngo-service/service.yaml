apiVersion: v1
kind: Service
metadata:
  name: ngo-service
  namespace: ngo
  labels:
    app: ngo-service
    app.kubernetes.io/name: ngo-service
    app.kubernetes.io/part-of: solidarytech
spec:
  type: ClusterIP
  selector:
    app: ngo-service
  ports:
    - name: http
      port: 80
      targetPort: 8081
      protocol: TCP
