# ════════════════════════════════════════════════════════════════
# DEPRECATED — este ConfigMap foi substituído pelo ExternalSecret.
#
# As variáveis "sqs-queue-url" agora são gerenciadas pelo
# External Secrets Operator via AWS Secrets Manager.
# Ver: gitops/donation-service/external-secret.yaml
#
# Este arquivo está vazio intencionalmente. O ArgoCD (prune: true)
# removerá o ConfigMap "donation-service-config" do cluster.
# ════════════════════════════════════════════════════════════════
